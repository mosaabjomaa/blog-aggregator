# Blog Aggregator (Gator)

A CLI tool written in TypeScript that aggregates RSS feeds and allows users to follow feeds and browse posts.

## Requirements

Before running the project, install:

- Node.js
- PostgreSQL

## Setup

Clone the repository:

```bash
git clone YOUR_REPO_URL
cd blog-aggregator
